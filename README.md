# ESP32MotiondetectionWIP
WiFi CSI + BLE motion detection using an ESP32 and a Qt6 desktop app. Sniffs channel state variance and Bluetooth RSSI variance, fuses both signals with debouncing, and visualizes live thresholds — no cameras or PIR sensors needed.
